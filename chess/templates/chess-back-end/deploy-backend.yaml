apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Chart.Name }}-Backend"
  labels:
    app: chess
spec:
  replicas: {{ .Values.chess-back-end.replicaCount }}
  selector:
    matchLabels:
      instance: pod-backend
  template:
    metadata:
      name: back
      labels:
        instance: pod-backend
    spec:
      containers:
      - name: back
        image: {{ .Values.chess-back-end.image }}
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: configmap-backend
      # tolerations:
      #  - key: "tool"
      #    operator: "Equal"
      #    value: "chess"
      #    effect: "NoSchedule"
      # affinity:
      #   nodeAffinity:
      #     requiredDuringSchedulingIgnoredDuringExecution:
      #       nodeSelectorTerms:
      #       - matchExpressions:
      #         - key: kubernetes.io/hostname
      #           operator: In 
      #           values:
      #           - tool-chess
