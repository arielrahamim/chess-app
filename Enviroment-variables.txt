BACKEND:
    MONGO_URI=*backend is reliant on this example : mongodb://$MONGO_INITDB_ROOT_USERNAME:$MONGO_INITDB_ROOT_PASSWORD@$MONGO_SERVICE:$MONGO_SERVICE_PORT
    GMAIL_PASS
    GMAIL_USER
    AWS_SECRET_ACCESS_KEY
    AWS_ACCESS_KEY_ID
    AWS_REGION
    GMAIL_USER: 
    GMAIL_PASS: 
    SERVER_PORT
    CLIENT_URL
FRONTEND:
    REACT_APP_SERVER_HOST=*backend host, needs to be imported before build command  
    REACT_APP_SERVER_PORT=*backend port, needs to be imported before build command 
MONGO
  ?